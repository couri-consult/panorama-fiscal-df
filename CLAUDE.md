# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Overview

Single-page dashboard ("Panorama Fiscal do DF") that visualizes fiscal indicators for the Brazilian Federal District (Distrito Federal). The dashboard itself is a static `index.html`; data is generated offline by Python scripts that pull from public APIs and a small manual XLSX, producing a `data.json` consumed at runtime by the page.

- **Live site:** https://couri-consult.github.io/panorama-fiscal-df/
- **Repo:** `couri-consult/panorama-fiscal-df` (GitHub Pages serves the `main` branch root)

## Architecture

```
panorama-fiscal-df/
├── index.html               # Dashboard (HTML + CSS + JS inline). Loads data.json.
├── data.json                # Generated by scripts/build_data.py. Versioned.
├── dashboard_alugueis_gdf.html  # Auxiliary standalone dashboard. Loads alugueis.json.
├── alugueis.json            # Generated by scripts/build_alugueis.py. Versioned.
├── styles/tokens.css        # CSS variables imported by index.html.
├── manual/
│   ├── panorama_manual.xlsx # Fallback source for sections without an API yet.
│   └── planilha-de-locacao-de-imoveis-...csv  # Source for the rentals dashboard.
├── capag/                   # Annual CAPAG CSVs from Tesouro (manual download).
│   └── capagdosestados{2018..2025}.csv
├── scripts/
│   ├── build_data.py        # Orchestrator. Reads .env, calls loaders, writes data.json.
│   ├── build_alugueis.py    # Reads the rentals CSV in manual/, writes alugueis.json.
│   ├── transforms.py        # Pure formatting / aggregation helpers.
│   └── loaders/
│       ├── _http.py         # GET with retry/backoff (SICONFI throttles hard).
│       ├── siconfi.py       # RREO + RGF (DF id_ente=53), row extractors.
│       ├── ibge.py          # SIDRA population (DF UF=53).
│       ├── capag.py         # Reads the local CSVs.
│       ├── transparencia.py # Portal da Transparência (FCDF) + web scrape for dotação.
│       └── manual.py        # Reads sheets from manual/panorama_manual.xlsx.
├── docs/
│   ├── arquitetura-2026-05-10.txt
│   └── proximos-passos.txt
├── .env                     # NOT committed (gitignored). Stores TRANSPARENCIA_TOKEN.
└── .gitignore               # Includes .claude/, __pycache__/, *.token, .env
```

### Runtime flow (browser)

1. `index.html` does `fetch('data.json')`.
2. Each top-level key in `data.json` (mirrors the old sheet names) is passed to a `renderXxx` function.
3. Chart.js draws charts (loaded via CDN). SheetJS is **no longer needed**.

### Build flow (Python, runs locally)

`python scripts/build_data.py` orchestrates:
1. Load `manual/panorama_manual.xlsx` as the baseline for every section.
2. Override sections with API/CSV data where available (CAPAG, IBGE, SICONFI RREO/RGF, Portal da Transparência).
3. Compute derived KPIs (e.g. investimento/RCL).
4. Write `data.json` to the project root.

**Resilience pattern:** Every API call is wrapped in `safely(...)` which falls back to the manual XLSX value if the call fails or returns empty. The dashboard always renders something.

### data.json shape

Mirrors the previous Excel sheet names for backward compatibility with the renderers in `index.html`. Top-level keys:

| Key | Renderer | Columns / fields |
|---|---|---|
| `kpis` | `renderKpis` | `chave`, `valor_bilhoes`, `sub`, `cor`. `chave` ∈ {orcamento_df, fcdf, investimento_rcl, capag, caixa, beneficios} |
| `investimentos` | `renderInvestimento` | `estado`, `pct`, `ranking`, `destaque` (ranking of all 27 UFs + média BR) |
| `caixa` | `renderCaixaChart` | `ano`, `caixa_liquido`, `nao_vinculado` (in reais; 5 anos from RGF Anexo 05) |
| `capag` | `renderCapag` | `indicador`, `nota`, `badge`, `valor`, `obs`, `conceito` (3 current indicators) |
| `capag_historico` | `renderCapagHist` | `ano`, `endividamento`, `poupanca`, `liquidez`, `consolidado` (2018–latest) |
| `pessoal` | `renderPessoal` | `chave`, `valor` — keys: atual_pct, alerta_pct, prudencial_pct, maximo_pct, rcl_bi |
| `clp_ranking` | `renderCLP` | `indicador`, `posicao`, `conceito` |
| `ppps` | `renderPPPs` | `nome`, `status` ("A contratar" / "Suspenso" / "Contratada") |
| `ppps_projecao` | `renderPPPProj` | `ano`, `despesas_ppp`, `rcl`, `pct` |
| `endividamento` | `renderEndividamento` | `periodo`, `rcl_ajustada`, `dc`, `dc_pct`, `brb_valor`, e `limites[]` com 3 itens (`chave` ∈ {dcl, oper_credito, garantias}, `nome`, `valor`, `pct`, `limite_pct`, `alerta_pct`, `limite_valor`, `norma`). `null` se a API do RGF falhar (a seção se auto-oculta). |
| `art_167a` | `renderArt167a` | `periodo`, `receita_corrente`, `despesa_corrente`, `pct`, `limite_pct` (95), `base_despesa`. Gatilho do art. 167-A da CF (despesa corrente ÷ receita corrente). `null` se o RREO Anexo 01 falhar (a seção se auto-oculta). |
| `ajuste_capag` | `renderAjusteCapag` | `nota_atual`, `meta` ("A+"), `poupanca_pct`, `meta_a_pct` (90), `meta_b_pct` (95), `base_receita_corrente`, `ajuste_a_bi`, `ajuste_b_bi`, `indicadores[]`. Caminho CAPAG C→A+ e esforço estimado (R$) na poupança corrente. `null` se faltar a poupança no CSV CAPAG. |
| `_meta` | (not rendered) | `updated_at`, periods, populacao_df, raw_siconfi, fcdf_dotacao_atualizada, `sources` |

`_meta.sources` shows the provenance of each key/sub-key (`manual`, `csv-local`, `siconfi-rreo-atual`, `portal-transparencia-scrape`, etc.) — useful for debugging.

## Data sources today

### Por campo no data.json

| Campo / KPI | Fonte | Notas |
|---|---|---|
| KPI orcamento_df | SICONFI RREO 2026/1 — `TotalDespesas (XII) = (X + XI)` × `DOTAÇÃO ATUALIZADA (e)` | id_ente=53 |
| KPI fcdf | Portal da Transparência web scrape (page `/orgaos/25915-...`) | LOA endpoint não existe; scrape do "Orçamento atualizado". UA Mozilla-like p/ passar do WAF CloudFront. |
| KPI investimento_rcl (valor) | SICONFI (calc: investimento_liquidado / RCL × 100) | RREO 2025/6 fechamento + RGF 2025/3 |
| KPI investimento_rcl (sub) | Aba `investimentos` do manual — posição do DF | "20º lugar no Brasil" derivado do ranking manual |
| KPI capag (nota) | CAPAG CSVs locais | Atualização anual quando STN publica |
| KPI caixa + tabela `caixa` (5 anos) | SICONFI RGF Anexo 05 (3º quad de cada ano) | `cod_conta=DisponibilidadeDeCaixaLiquidaAposRP` + conta TOTAL (IV) ou (III) |
| Seção `endividamento` (3 limites) | SICONFI RGF Anexos 02 (DCL), 03 (Garantias) e 04 (Operações de Crédito) — período `DIVIDA_RGF_*` (mais recente publicado, ≠ do RGF de fechamento) | Anexo 02 (coluna `Até o Nº Quadrimestre`): `DividaConsolidadaLiquida`, `DividaConsolidada`, `Percentual…SobreARCL`, `LimiteDefinidoPorResolucaoDoSenadoFederal`. Anexo 03 (mesma coluna): `TotalGarantiasConcedidas`, `PercentualDoTotalDasGarantiasSobreARCL`, `LimiteDefinidoPorResolucaoDoSenadoFederal`. Anexo 04 — tabela "Apuração do Cumprimento de Limites" (colunas `VALOR` e `% SOBRE A RCL AJUSTADA`): `TotalConsideradoParaFinsDaApuracao…`, `LimiteGeralDefinidoPorResolucaoDoSenadoFederalParaAsOperacoesDeCredito…`, `LimiteDeAlertaOperacoesCredito`. Operação BRB/FGC (16% e 22%) só entra a partir do RGF 2026/2. |
| KPI beneficios (valor) | Dataset `beneficiometro/renuncias-{ano}.txt` (dados.df.gov.br) | Total agregado do TXT publicado pela SEEC; fallback p/ manual XLSX se ausente |
| KPI beneficios (sub) | calc: beneficios / receita_impostos × 100 | receita_impostos vem da aba `kpis` linha 8 (manual, RREO Anexo 8 — não disponível no SICONFI) |
| Seção `beneficios_detalhado` (3 gráficos) | Dataset `beneficiometro/renuncias-{ano}.txt` | Agregações: por tributo, top 10 benefícios, top 10 beneficiários com setor inferido |
| Seção `riscos_fiscais` (2 cards) | Aba `riscos_fiscais` do manual XLSX | Anexo XII do PLDO (atualização anual). Agregados: total, por bloco, por dependência (STF/Contratual/Operacional/Cambial/Macro), top 10 individual. |
| Seção `art_167a` (gatilho 95%) | SICONFI RREO Anexo 01 (fechamento) — `extract_rreo_correntes` | `cod_conta` `ReceitasCorrentes` (coluna `Até o Bimestre (c)`) e `DespesasCorrentes` (coluna `DESPESAS EMPENHADAS ATÉ O BIMESTRE (f)`). pct = despesa/receita. Teto de 95% (art. 167-A CF, EC 109/2021) — gatilho das vedações dos Decretos 48.509/48.549 de 2026. |
| `investimentos` (ranking 27 UFs) | manual | Ainda não automatizado (requer 54 chamadas SICONFI) |
| `pessoal.atual_pct`, `rcl_bi` | SICONFI RGF Anexo 01 (DTP) | linha `ReceitaCorrenteLiquidaLimiteLegal` + `DespesaComPessoalTotal`. Inseridos via upsert no array de pessoal. |
| `pessoal.alerta_pct/prudencial_pct/maximo_pct` | Manual (limites legais da LRF) | Aba `pessoal` |
| `capag` indicadores (nota + valor) | CSVs locais (CAPAG) | nota/valor recomputados; `obs` e `conceito` mantidos do manual |
| `capag` indicadores (obs + conceito) | Aba `capag` do manual | Textos descritivos (mensagens "Para nota A: < 60%" etc) |
| `capag_historico` | Gerado dos CSVs locais (transforms) | — |
| `clp_ranking` | Aba `clp_ranking` do manual | 9 indicadores do Pilar Solidez Fiscal — atualização anual quando CLP publica |
| `_meta.clp_pos_geral` | Aba `clp_meta` linha `pos_geral` do manual | Texto livre p/ posição/delta (ex: "20º (↓8 vs 2024)") |
| `ppps`, `ppps_projecao` | Aba `ppps` / `ppps_projecao` do manual | Textual + forecast — manual ok |
| `agenda` | Aba `agenda` do manual | Eixos da agenda de recuperação fiscal — editável |

### O que cada aba do `manual/panorama_manual.xlsx` contribui

Após a limpeza de 18/05/2026, o manual contém **só o que o painel realmente lê**:

| Aba | Colunas usadas | Função |
|---|---|---|
| `kpis` | `chave`, `valor_bilhoes`, `sub`, `cor` | Define a ordem dos 6 KPIs. Para `orcamento_df`, `fcdf`, `investimento_rcl`, `capag`, `caixa` os valores são sobrescritos pela API (linhas servem só de anchor). **`beneficios` é a única linha com valor manual editável**. |
| `investimentos` | `estado`, `pct`, `ranking`, `destaque` | Ranking dos 27 UFs (manual integral) |
| `capag` | `indicador`, `obs`, `conceito` | Textos descritivos — `nota`/`valor` vêm do CSV |
| `pessoal` | `chave`, `valor` | Apenas limites LRF (`alerta_pct`, `prudencial_pct`, `maximo_pct`). `atual_pct` e `rcl_bi` são adicionados pela API. |
| `clp_ranking` | `indicador`, `posicao`, `conceito` | 9 indicadores do CLP Pilar Solidez Fiscal |
| `clp_meta` | `chave`, `valor` | `pos_geral` (texto p/ posição no pilar), `pos_geral_obs`, `ano_ranking` |
| `ppps` | `nome`, `status` | Lista de PPPs (contratadas / a contratar / suspenso) |
| `ppps_projecao` | `ano`, `despesas_ppp`, `rcl`, `pct` | Projeção plurianual |
| `agenda` | `eixo`, `cor`, `titulo`, `item` | 3 eixos da Agenda de Recuperação Fiscal |
| `riscos_fiscais` | `bloco`, `categoria`, `descricao`, `nome_curto`, `valor`, `dependencia` | Anexo XII do PLDO (~17 linhas). Atualização anual. |

Sumiram (não eram usadas): `caixa` (vem da API), `capag_historico` (gerado dos CSVs), `Planilha2` (lixo).

## SICONFI specifics (lessons learned)

- **Entidade do DF**: usar `id_ente=53` (cod_ibge da UF). NÃO usar 5300108 — esse é Brasília como **município** (esfera M).
- **Documento de referência**: `Cod_instituicoes_siconfi.pdf` (no site siconfi.tesouro.gov.br). DF Executivo aparece como `53EX`, cod_ibge 53.
- **Rate limit**: o Oracle ORDS pool retorna HTTP 429 frequentemente. `_http.py` faz retry com backoff exponencial. `siconfi.py` adiciona um `time.sleep(3)` entre chamadas.
- **MCASP atual vs antigo**: a numeração das linhas mudou. Linha que o usuário pode chamar de "(VIII)" no RREO antigo corresponde a `TotalDespesas (XII)` no formato atual. Vide `extract_rreo_balanco` em `siconfi.py`.
- **RGF Anexo 05**: o `cod_conta` é `DisponibilidadeDeCaixaLiquidaAposRP` (com `AposRP` no final). O label da linha TOTAL mudou entre 2022 (`TOTAL (III) = (I + II)`) e 2023+ (`TOTAL (IV) = (I + II + III)`) — `extract_rgf_caixa` trata os dois.

## Portal da Transparência specifics

- **Token**: requer registro em https://portaldatransparencia.gov.br/api-de-dados/cadastrar-email com selo gov.br Prata ou Ouro. Token é enviado por email. Pedir mais de uma vez **invalida o anterior**.
- **Token storage**: arquivo `.env` (gitignored). Variável `TRANSPARENCIA_TOKEN`. `build_data.py` carrega via `_load_dotenv()`.
- **LOA / dotação atualizada**: a API `/api-de-dados` só expõe empenhado/liquidado/pago. Para a dotação atualizada do FCDF (mostrada na página `/orgaos/25915-...`) usamos web scraping em `fetch_fcdf_dotacao_atualizada()`. Frágil a mudanças de layout — se quebrar, ajustar o regex.

## Rentals dashboard (`dashboard_alugueis_gdf.html`)

A **standalone** auxiliary dashboard, fully independent of `index.html` / `data.json` / the
`build_data.py` pipeline. Analyzes the DF Executive's property-lease contracts (2nd and 3rd
quarters of 2025).

- **Data source:** `manual/planilha-de-locacao-de-imoveis-2-e-32025-para-processo-em-cumprimento-a-lei-distrital.csv`
  — a `;`-delimited CSV, **latin-1** encoded, 11 columns, ~149 contracts.
- **Build:** `python scripts/build_alugueis.py` reads the CSV, parses/curates it, and writes
  `alugueis.json` to the project root.
- **Runtime:** the HTML does `fetch('alugueis.json')` inside `init(DATA)`. It must be served over
  HTTP (GitHub Pages serves it at `/dashboard_alugueis_gdf.html`; locally `python -m http.server`).
  KPIs and the R$/m² stats table are computed in-browser from the JSON.

`build_alugueis.py` owns the curation:
- **Short org name** (`oc`): rule-based — strips ` do Distrito Federal`, maps `Secretaria de
  Estado de X` → `SE X`, `Administração Regional` → `RA`, `Instituto de` → `Inst.`.
- **Owner grouping** (`gp`): keyword-based — consolidates corporate-family variants
  (Paulo Octávio/PO 700, Sarkis, Phenícia, Estrutural, Serra Bonita); otherwise `gp` = cleaned
  owner name (CNPJ parenthetical stripped).

CSV column order (0-indexed): `0` órgão, `1` endereço, `2` descrição, `3` finalidade,
`4` valor mensal, `5` R$/m² total, `6` R$/m² útil, `7` prazo+data, `8` gasto total,
`9` pessoal, `10` proprietário+CNPJ.

**To update:** replace the CSV in `manual/` (keep the same filename), run
`python scripts/build_alugueis.py`, commit `alugueis.json`.

## Running / developing

```bash
# Setup (uma vez)
pip install requests openpyxl pandas pypdf
# Criar .env com TRANSPARENCIA_TOKEN (não commitar)
echo "TRANSPARENCIA_TOKEN=<seu_token>" > .env

# Atualizar dados
python scripts/build_data.py

# Ver localmente
python -m http.server 3000
# → http://localhost:3000

# Publicar
git add data.json [outros arquivos modificados]
git commit -m "atualiza dados"
git push
```

GitHub Pages republica em ~30-90s após o push. Para forçar reload no browser: ctrl+shift+R.

## Conventions

- All UI copy and data labels are in Portuguese (pt-BR). Preserve the language when editing.
- Color system lives in `styles/tokens.css` (CSS variables) — reuse them rather than adding new hex values.
- Keep the single-file structure of `index.html`; do not introduce a bundler or framework.
- Python: stdlib + `requests`, `openpyxl`, `pandas`. No build, no virtualenv enforcement. Type hints optional but welcome.
- Number formatting: Brazilian style (`.` for thousands, `,` for decimal). `_parse_br_number()` in `transparencia.py` handles strings; `num()` in `index.html` handles them in JS.

## Design system

Fonts loaded from Google Fonts (`<link>` tags in `<head>`):
- Titles / KPI values / card-titles / section-titles: **Poppins** 600, 700 → `var(--font-title)`
- Body / nav / legends: **Inter** 400, 500 → `var(--font-body)`

The CSS variables live in `styles/tokens.css` and a fallback block at the top of `<style>` in `index.html`. Tokens include:

| Variable | Value | Usage |
|---|---|---|
| `--g800` | `#1B5E20` | Header bg, tooltip bg |
| `--g700` | `#2E7D32` | Section titles, card titles, links |
| `--g600` / `--g500` | `#4CAF50` | Primary green |
| `--g300` | `#66BB6A` | Hover states |
| `--g200` | `#A5D6A7` | Section title border |
| `--g100` | `#E8F5E9` | Table headers bg, badge bg |
| `--red` | `#c0392b` | Negative indicators |
| `--amber` | `#c47a10` | Warning indicators |
| `--blue` | `#1a5fa6` | Informational indicators |
| `--lgray` / `--muted` | `#6B7280` | Secondary text |
| `--bg` | `#F5F5F5` | Page background |
| `--card` | `#FFFFFF` | Card / KPI background |
| `--border` | `#E5E7EB` | Card borders |
| `--text` | `#1A1A1A` | Primary text |
| `--font-title` | `'Poppins',sans-serif` | Title font stack |
| `--font-body` | `'Inter',sans-serif` | Body font stack |
| `--radius` | `14px` | Card border-radius |
| `--shadow` | `0 1px 3px rgba(0,0,0,0.06), 0 1px 2px rgba(0,0,0,0.04)` | Soft shadow |
